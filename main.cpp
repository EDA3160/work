#include<iostream>
#include "database/database.h"
#include "parser/parser.h"

int main()
{
    std::vector<mos> Moses);
    parser p;
    p.parsers(Moses);
    std::cout << "hello" <<std::endl;
    return 0;
}